package com.example.demo.screen;

/**
 * Enum representing the different types of screens in the game.
 * Each constant corresponds to a specific screen that can be navigated to.
 */
public enum ScreenType {
    MAIN_MENU,
    LEVEL_ONE,
    LEVEL_TWO,
    LEVEL_ENDLESS_MODE
}
